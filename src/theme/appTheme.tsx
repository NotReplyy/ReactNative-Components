import { StyleSheet } from 'react-native';


export const appTheme = StyleSheet.create({
    globalMargin: {
        marginHorizontal: 20,
    },
    title: {
        fontSize: 30,
        fontWeight: 'bold',
    },
});
